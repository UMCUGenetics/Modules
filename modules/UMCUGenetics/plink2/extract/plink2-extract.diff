Changes in component 'nf-core/plink2/extract'
'modules/nf-core/plink2/extract/environment.yml' is unchanged
'modules/nf-core/plink2/extract/meta.yml' is unchanged
Changes in 'plink2/extract/main.nf':
--- modules/nf-core/plink2/extract/main.nf
+++ modules/nf-core/plink2/extract/main.nf
@@ -3,9 +3,9 @@
     label 'process_low'
 
     conda "${moduleDir}/environment.yml"
-    container "${ workflow.containerEngine in ['singularity', 'apptainer'] && !task.ext.singularity_pull_docker_container ?
+    container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
         'https://depot.galaxyproject.org/singularity/plink2:2.00a2.3--h712d239_1' :
-        'quay.io/biocontainers/plink2:2.00a2.3--h712d239_1' }"
+        'biocontainers/plink2:2.00a2.3--h712d239_1' }"
 
     input:
     tuple val(meta), path(pgen), path(psam), path(pvar), path(variants)
@@ -13,7 +13,8 @@
     output:
     tuple val(meta), path("*.pgen")    , emit: extract_pgen
     tuple val(meta), path("*.psam")    , emit: extract_psam
-    tuple val(meta), path("*.pvar.zst"), emit: extract_pvar
+    tuple val(meta), path("*.pvar.zst"), emit: extract_pvar_zst
+    tuple val(meta), path("*.pvar")    , emit: extract_pvar
     tuple val("${task.process}"), val('plink2'), eval("plink2 --version 2>&1 | sed 's/^PLINK v//; s/ 64.*\$//'"), topic: versions, emit: versions_plink2
 
     when:
@@ -33,12 +34,16 @@
         --extract $variants \\
         --make-pgen vzs \\
         --out ${prefix}
+
+    plink2 --zst-decompress ${prefix}.pvar.zst > ${prefix}.pvar
     """
+
     stub:
     def prefix = task.ext.prefix ?: "${meta.id}"
     """
     touch ${prefix}.pgen
     touch ${prefix}.psam
     touch ${prefix}.pvar.zst
+    touch ${prefix}.pvar
     """
 }

'modules/nf-core/plink2/extract/tests/main.nf.test.snap' is unchanged
'modules/nf-core/plink2/extract/tests/nextflow.config' is unchanged
'modules/nf-core/plink2/extract/tests/main.nf.test' is unchanged
************************************************************
