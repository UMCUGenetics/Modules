process {

    withName: BCFTOOLS_NORM {
        ext.args = [
            "-m -", // Split multi-allelic sites
            "--output-type z",
            "--write-index=tbi"
            ].join(' ').trim()
        ext.prefix = { "${meta.id}_norm"}
    }
}
